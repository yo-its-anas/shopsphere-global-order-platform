# Kubernetes Base

Holds common namespace and safe resource-governance manifests. The foundation base creates only ShopSphere namespaces, ResourceQuotas, and LimitRanges; it deploys no business service, database, cache, message broker, identity provider, Jenkins workload, or monitoring stack.
