# PoC Overlay

Targets the resource-constrained `shopsphere-poc` single-node kind cluster on one Google Cloud VM. Multiple replicas share the same node and physical host, so they do not provide host-level high availability. The Day 1 overlay applies only namespaces and conservative resource governance.
