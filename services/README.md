# Services

Contains independently deployable backend boundaries. Each service will own its API, persistence mappings, migrations, tests, container definition, and operational metadata while sharing only governed contracts and utilities.
