[build-system]
requires = ["setuptools==80.9.0"]
build-backend = "setuptools.build_meta"

[project]
name = "shopsphere-api-gateway"
version = "0.1.0"
description = "Versioned ShopSphere API gateway with transport-only customer capability routing."
requires-python = ">=3.10,<3.15"
dependencies = [
  "fastapi==0.139.2",
  "httpx2==2.7.0",
  "uvicorn==0.51.0",
]

[project.optional-dependencies]
dev = [
  "bandit==1.9.4",
  "black==26.5.1",
  "httpx2==2.7.0",
  "pytest==9.1.1",
  "ruff==0.15.22",
]

[tool.setuptools.packages.find]
include = ["app*"]

[tool.black]
line-length = 100
target-version = ["py310"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I", "UP", "B", "S"]

[tool.ruff.lint.per-file-ignores]
"tests/**/*.py" = ["S101"]

[tool.pytest.ini_options]
addopts = "-q --strict-markers"
testpaths = ["tests"]

[tool.bandit]
exclude_dirs = ["tests"]
