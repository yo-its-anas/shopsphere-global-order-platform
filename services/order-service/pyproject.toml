[build-system]
requires = ["setuptools==80.9.0"]
build-backend = "setuptools.build_meta"

[project]
name = "shopsphere-order-service"
version = "0.1.0"
description = "Enterprise shopping-cart and order processing boundary."
requires-python = ">=3.10,<3.15"
dependencies = [
  "alembic==1.18.4",
  "anyio==4.12.1",
  "fastapi==0.139.2",
  "httpx2==2.7.0",
  "psycopg[binary]==3.3.4",
  "pydantic==2.13.4",
  "PyJWT[crypto]==2.13.0",
  "SQLAlchemy==2.0.51",
  "uvicorn==0.51.0",
]

[project.optional-dependencies]
dev = [
  "bandit==1.9.4",
  "black==26.5.1",
  "aiosqlite==0.22.1",
  "pytest==9.1.1",
  "pytest-cov==7.0.0",
  "ruff==0.15.22",
]

[tool.setuptools.packages.find]
include = ["app*"]

[tool.black]
line-length = 100
target-version = ["py310"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I", "UP", "B", "S"]

[tool.ruff.lint.per-file-ignores]
"tests/**/*.py" = ["S101"]

[tool.pytest.ini_options]
addopts = "-q --strict-markers --cov=app --cov-report=term-missing"
testpaths = ["tests"]

[tool.bandit]
exclude_dirs = ["tests"]
